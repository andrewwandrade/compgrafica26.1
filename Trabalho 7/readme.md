### Proposta do trabalho

Implementar a função "void draw(Triangle<Varying> triangle)". 
Para cada pixel da rasterização, calcule as coordenadas baricêntricas. Use essas coordenadas para interpolar os atributos (use a função mix_triangle). Pinte o pixel com os atributos interpolados

### Observações

O trabalho utiliza bibliotecas prontas preparadas pelo professor.